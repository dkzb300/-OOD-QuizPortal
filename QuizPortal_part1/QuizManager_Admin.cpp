
#include "stdafx.h"
#include "QuizManager_Admin.h"

bool QuizManager_Admin::Run_quizManager(Account* an_account) {
	my_account = an_account;
	int commandInt = 0;
	int maxCommand;

	/*
	my_Command->reset();

	while(true) {
		maxCommand = 0;
		cout << "¡å ¸í·É:" << endl;
		cout << "[ Exit    ] : " << maxCommand++ << endl;
		cout << "[ Quiz    ] : " << maxCommand++ << endl;
		if (my_account == NULL) {
			my_Command->commandQuizValidation[maxCommand] = 2;
			cout << "[ Create  ] : " << maxCommand++ << endl;
			my_Command->commandQuizValidation[maxCommand] = 3;
			cout << "[ Log_In  ] : " << maxCommand++ << endl;
		}
		else {
			my_Command->commandQuizValidation[maxCommand] = 4;
			cout << "[ Log_Out ] : " << maxCommand++ << endl;
		}
	}
	*/

	return false;
}